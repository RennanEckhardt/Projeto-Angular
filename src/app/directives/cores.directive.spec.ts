import { CoresDirective } from './cores.directive';

describe('CoresDirective', () => {
  it('should create an instance', () => {
    const directive = new CoresDirective();
    expect(directive).toBeTruthy();
  });
});
